certifi==2024.2.2
charset-normalizer==3.3.2
idna==3.6
numpy
requests==2.31.0
setuptools==69.1.1
urllib3==2.2.1
websockets==12.0
wheel==0.42.0
python-dotenv
soundfile
sounddevice
# Packages that require build tools on Windows (commented out):
# gevent==24.2.1  # Requires build tools and has Python 3.13 compatibility issues
# greenlet==3.0.3  # Dependency of gevent
# zope.event==5.0  # Dependency of gevent
# zope.interface==6.2  # Dependency of gevent
# PyAudio  # Requires build tools
# playsound  # May work but can be problematic
# simpleaudio  # Requires build tools
# cffi  # Requires build tools
